{
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start:prod": "cd server && npm run start:prod",
    "test": "turbo test"
  },
  "prettier": {
    "tabWidth": 2,
    "singleQuote": true
  },
  "devDependencies": {
    "turbo": "^1.10.16"
  }
}
